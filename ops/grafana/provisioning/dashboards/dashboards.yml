apiVersion: 1
providers:
  - name: pipeline
    orgId: 1
    folder: Video Pipeline
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: false      # dashboards are code; edit the JSON in the repo
    options:
      path: /var/lib/grafana/dashboards
